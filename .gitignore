# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.claude
.vercel

# Local env files — real secrets live here, never committed.
.env
.env.*
!.env.example
*.traineddata
.tmp-ocr-images

# Eval harness output
eval/results.json

# Stryker mutation testing (temp sandboxes + HTML report output + the
# break-disabled config copy CI generates for PR-scoped runs)
.stryker-tmp
reports
.stryker-pr.config.json
